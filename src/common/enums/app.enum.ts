export enum EnvEnum {
  Dev = 'development',
  Staging = 'staging',
  Production = 'production',
}

export enum LOCALES {
  EN = 'en-US',
  VI = 'vi',
}

export enum ExportExcelType {
  CSV = 0,
  XLSX = 1,
}
